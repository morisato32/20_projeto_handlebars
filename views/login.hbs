   

<section class="container">
     {{#if errors.length}}
        <div class="alert alert-danger message-erro">
            {{#each errors}}
                <p>{{this}}</p>
            {{/each}}
        </div>
    {{/if}}

    <div class="row">
        <div class="grid-4 disappear"></div>

        <div class="grid-4 card-form mt-9">
            <h4 class="text-gradient">Login</h4>
            <form action="/user/login" method="post" class="mt-4">
                <div>
                    <label for="email">E-mail:</label>
                    <input type="email" name="email"  id="email"  class="mt-1">
                </div>

                <div class="mt-2">
                    <label for="senha">Senha:</label>
                    <input type="password" name="senha" id="senha" class="mt-1">
                </div>

              <div class="mt-4">
                    <input type="submit" value="Login" class="btn-primary">
                </div>

                 <p>Não tem conta? <a href="/user/cadastrar" class="text-darkgreen mt-2">Clique aqui</a></p>
            </form>
        </div>

        <div class="grid-4 disappear"></div>
        
    </div>
</section>
<script src="../js/errorLogin.js"></script>
